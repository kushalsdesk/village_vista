services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: development # Use "production" for production builds
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: ["npm", "run", "dev"]
